import { RegionItem } from "../interfaces";

export const regions: Record<string, RegionItem> = {
    EU: {
        variant: 'green',
        continent: 'AS'
    },
    SA: {
        variant: 'blue',
        continent: 'EU'
    }
}